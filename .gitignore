.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Runtime data/caches (regenerated via `hustring build-data`)
# The container writes to data/graph, so the whole data dir is ignored.
data/
*.parquet
*.npz
build_manifest.json

# Private working notes (kept local, not published)
docs/TECH_DEBT.md
